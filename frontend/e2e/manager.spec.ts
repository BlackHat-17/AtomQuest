/**
 * E2E stubs — Manager journeys
 * Replace each test.todo() with a full implementation when ready.
 */

import { test } from '@playwright/test';

test.describe('Manager journeys', () => {
  test.todo('login as manager');
  test.todo('view team dashboard');
  test.todo('approve goal sheet');
});
